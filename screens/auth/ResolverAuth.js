import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ResolverAuth = ({navigation}) => {
  return (
    <View>
      <Text>ResolverAuth</Text>
    </View>
  )
}

export default ResolverAuth

const styles = StyleSheet.create({})